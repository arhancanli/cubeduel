**What this changes, and why**

<!-- The reasoning matters more than the diff. What was wrong, or what was missing? -->

**How it was verified**

<!-- Which layer would have caught this failing? See CONTRIBUTING.md. -->

- [ ] `npm run typecheck` / `npm run lint` / `npm test`
- [ ] `npm run build` and `npm run check:bundle`
- [ ] Browser suites (`npm run e2e`) — if this touches the solve loop or a screen
- [ ] Integration (`npm run integration`) — if this touches the database

**Does it make any new claim to the player?**

<!-- A statistic, a rating, a verdict. If so: what sample size supports it, and
     what happens when there isn't enough data? -->
